services:
  swagger:
    image: swaggerapi/swagger-ui
    profiles:
      - hafbe
      - apps
    environment:
      URLS: >-
        [{url: "/hafbe-api/", name: "HAF Block Explorer"},
         {url: "/balance-api/", name: "Balance Tracker"},
         {url: "/reputation-api/", name: "Reputation Tracker"},
         {url: "/hafah-api/", name: "HAfAH"}]
    networks:
      haf-network:
    #healthcheck:
    #  test: ["CMD-SHELL","curl -f localhost:8080"]
