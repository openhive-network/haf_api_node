# HAProxy scrape configuration (uses built-in Prometheus exporter)
# Include in prometheus.yml via scrape_config_files or copy to scrape_configs section

- job_name: "haproxy"
  scrape_interval: 5s
  static_configs:
    - targets: ["haproxy:8000"]
  metrics_path: /metrics
