# Drone JSON-RPC caching proxy scrape configuration
# Include in prometheus.yml via scrape_config_files or copy to scrape_configs section

- job_name: "drone"
  scrape_interval: 5s
  static_configs:
    - targets: ["drone:9000"]
  metrics_path: /metrics
